[package]
name = "DeskSync"
version = "1.0.0"
edition = "2024"

[dependencies]
notify = "8.2.0"
notify-debouncer-mini = "0.7.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

[build-dependencies]
ico = "0.5.0"
resvg = "0.47.0"
winres = "0.1.12"
